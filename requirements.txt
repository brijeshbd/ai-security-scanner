# AI Security Scanner — Python dependencies
# ==========================================
# Core (always needed)
ollama          # local AI via Ollama — free, no API key
reportlab       # PDF report generation

# Optional providers — install only the one you want to use
# anthropic     # Claude API  → pip install anthropic
# openai        # OpenAI API  → pip install openai
# groq          # Groq API    → pip install groq